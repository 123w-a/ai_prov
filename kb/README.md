# 知识库目录说明

## 已纳入索引

`0_总纲` 至 `4_特殊人群膳食指南` 下的 PDF 会由 `build_kb_rag.py`
按允许层级扫描并写入向量库。

## 未纳入索引

- `_未入库/餐厅指南附件_常见富含营养素的食物举例.docx`

该文件为 `.docx`，当前索引流水线只解析 PDF 和 Markdown。为避免静默跳过并
遵守“不新增依赖”的约束，本次不引入 Word 解析器，也不把未解析内容伪装成
已入库语料。后续如需纳入，应先增加明确的 DOCX 解析与测试。
