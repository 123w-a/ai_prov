# 项目协作约束

## 文字输出链路冻结

- 当前文字输出没有出现可复现错误时，禁止主动修改 `max_tokens`、模型文字收口、流式 token 转发、兜底文案和截断处理。
- 不得以“顺手优化”“统一风格”或预防性重构为由改动上述逻辑。
- 只有实际出现截断、空回答、回答缺失、流式输出异常等可复现问题时，才允许处理；处理前必须先说明复现证据、影响范围和拟修改内容，并取得用户明确同意。
- 双兜底策略属于文字输出链路的既有保护，未取得明确同意不得删除、绕过或降低其生效范围。
