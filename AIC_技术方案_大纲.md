# AIC 第八届 · 算法创新赛（AC2/AI+场景创新）技术方案 PDF 大纲

> **用途**：省赛报名需提交《技术方案》PDF（≤10M）。本文件是**内容大纲 + 可直接套用的段落草稿**，按评分公式 `(0.5A选题 + 0.2B技术难度 + 0.3F原创性) × (0.5C创新 + 0.4D完成 + 0.1E其他)` 组织，保证每项评分都有实证可讲。
> **提交前**：替换全部【】占位符、补代码行号、加图表、删掉"（答辩点）"等提示括号。
> **公平性红线**：全文不得出现校名 / 学校 Logo / 指导教师姓名（评分会扣分甚至取消资格）。

---

## 一、选题定位与创新点（对应评分 A「选题定位 0.5」+ C「创新度 0.5」）

### 1.1 一句话定位（放封面下方，答辩开场句）
> 把"菜谱搜索器"升级为**带健康护栏的多模态膳食决策 Agent**——拍一张冰箱照，在过敏原 / 慢病忌口 / 控糖控盐 / 热量目标之内，给出可验证、可溯源、可长期跟随的一餐。

### 1.2 问题（Why Now）
- 传统食谱应用只给做法，不约束"这个菜适不适合我吃"。
- 慢病人群 / 控糖人群 / 过敏人群做菜时最怕"踩坑"，但网上食谱鱼龙混杂，缺少**可执行的个性化健康护栏**。
- 已有菜谱搜索器缺**结构化校验**，模型"自觉"不靠谱——需要一个能**硬约束**而不是靠提示词约束的方案。

### 1.3 4 大核心创新点（每点讲 3 句话：做了什么 → 技术难点 → 比现有强在哪）

| # | 创新点 | 一句话技术说明 | 代码锚点 |
|---|---|---|---|
| 1 | **零输入「冰箱到餐桌」多模态闭环 Agent** | 拍照→视觉识别→联网搜→结构化卡片→成品图匹配，全程无需用户手打菜名 | `main.py` / `agent_graph.py` / `app.py` |
| 2 | **健康约束三层硬执行护栏** | 不靠模型自觉：schema 校验(L1)+Python 排序(L2)+健康约束校验器(L3)；违规拦截+回灌重生成 | `nutrition_rules.py` + `agent_graph.py` |
| 3 | **全链路可信标识 / 透明标注** | AI 生成图与成品图透明区分，菜谱来源链接可溯源，对应《AIGC 标识办法》合规 | `agent_schemas.py` / `agent_graph.py` / `app.py` |
| 4 | **自适应画像补采 + 三层健康记忆** | 充分性门控主动追问≤2 问；三层记忆与三层存储天然对齐，实现"越用越懂你" | `agent_graph.py` + `agent_prompts.py` |

（答辩点）创新点 2 是最强项——可现场 A/B 对照演示"开护栏 vs 关护栏"违规率骤降。建议技术方案里配一张**三层护栏流程图**（mermaid）。

---

## 二、技术与架构（对应 B「技术难度 0.2」+ D「完成度 0.4」）

### 2.1 系统总体架构
> 分层描述：前端(React 19+TS+Vite) → API 层(FastAPI 多路由) → Agent 编排(LangGraph) → 工具层(Agent Tools) → 存储层(双层)。配一张架构总览图。

### 2.2 Agent 核心循环（技术深度重点）
- 用 **LangGraph** 显式状态机实现"可观察、可中断、可持久化的可控循环"（区别于黑盒 ReAct 隐式循环）。
- 节点流：多模态输入 → 思考(chef_think) → 工具调用 → 结构化收尾。
- 判定是否走工具：看 `AIMessage.tool_calls` 字段是否有值（有→run_tools；无→结构化节点）。

（答辩点）这里能讲出"我看懂了返回结构"而非背 API，是拉开差距的地方。

### 2.3 图片全链路（含 AI 透明标注）
- 三源成品图：搜索图 / AI 生成图 / 用户上传图，各自透明标注。
- 防错配：**菜名定稿后才搜图**。
- 轻量级多模态：不扛全套视觉大模型，走"识别+检索"降本。

### 2.4 双层会话存储 + 记忆
- `checkpoint.db`（LangGraph 图状态/断点）与 `sessions/*.json`（业务结构化）配合。
- 历史浓缩机制：压缩时**结构化字段保留 + 自然语言 LLM 摘要**，避免失忆。
- 硬件约束：OpenAI 要求 `ToolMessage.tool_call_id` 与上方 `AIMessage.tool_calls[].id` 配对，故压缩需"连坐删"以规避 400（答辩硬知识点）。

### 2.5 工具链（Agent Tools）
- 列举实际工具：web_search / 菜谱检索 / 成品图生成(image_gen，多级回退+webp) / **附近餐厅 POI 检索(nearby_food，高德+mock 双轨)** / 热量查询 / 运动当量换算 / 营养知识库检索。
- 强调"菜系护栏"由我们产品自研（高德不提供）。

### 2.6 RAG（若仍用，可单列；否则标注"见佐证"）
- 自建检索库（离线索引 / bge embedding / 混合检索 / 重排 / 引用标注）——**这套是硬深度**，若篇幅允许建议单开一节并挂出 402 chunks 数据。

### 2.7 多模型可插拔 + 双兜底配置
- 显式参数 > `CHEF_PROVIDER` 环境变量 > 配置默认（三级兜底）。
- 答辩记忆点：**"有参优先 + 无参读 env"** 的容错设计。

### 2.8 工程规模（一句话给体量，附文件树或表格）
> 全栈约 2.1 万行：Python 12583 行(94 文件) + 前端 8415 行(19 文件)。核心 Agent 内核约 3000 行；测试 2303 行、评测脚本 463 行。严格排除依赖与构建产物。
> （答辩口径拆分：全栈总量 / 核心业务代码≈1.7万 / Agent内核≈3000）

---

## 三、验证数据与成果（对应 D「完成度 0.4」最强证据）

### 3.1 客观技术验证（自动化可先出）
- 测试用例集：健康约束用例(建议 50+) + 对抗用例(建议 10+)，出 JSON 原始数据。
- A/B 硬校验开关对照：开护栏 vs 关护栏的违规率/召回率对比图表 + CSV 原始数据。
- 前端后端跑通截图 + 分阶段耗时/成功率埋点(可选 `logs/metrics.jsonl`)。

### 3.2 用户验证（若已做，补数据位）
- 问卷 N≥30 → 痛点分布 3-4 张图。
- 深度访谈 5-8 人 → 3 条可引用金句。
- 试用测评 10-15 人 × 3 天 → SUS / NPS / 盲测偏好率。
- 营养师 / 医生书面背书（可选高价值佐证）。

### 3.3 佐证材料（对应"材料规范性"E）
- 【软件著作权证书】/【专利受理】/【用户反馈】/【试点应用证明】等，合并为一个 PDF。

---

## 四、合规与安全（对应 E「其他/规范性」+ 评委必问）

### 4.1 数据与隐私
- 用户对话 / 食材图片存本地(或私有 OSS + 签名 URL)，不对外泄露。
- 展示前净化个人会话 / 偏好脱敏。

### 4.2 医疗合规边界（主动划清，防被扣"越权行医"）
- 明确：**本产品提供"膳食建议"，不构成医疗诊断 / 治疗 / 用药建议**。
- 免责三处：弹窗 + 页脚 + 卡片小字。
- 慢病忌口规则来自公开膳食指南（可溯源），不替代医生。

### 4.3 高德 API 合规（评委若问 nearby_food 来源）
> 标准话术："本项目按**个人开发者条款**使用高德基础搜索 API（免费月配额内，仅学习研究 / 答辩演示用）；工程上做**离线 mock 兜底**——接口失败 / 超配额时自动降级到本地数据集，保证可用性，符合个人开发者使用规范。"
> （若答辩被追问"上线怎么办"：明确"正式商用需企业技术服务许可，当前为学习演示用途"）

### 4.4 AIGC 透明标注（对应《人工智能生成合成内容标识办法》）
- AI 生成图标注"AI 生成示意图（非真实成品照）"。
- 成品图区分来源（搜索图 / AI 图 / 用户图），给出来源链接。

---

## 五、竞品分析与价值（对应 C「创新度」的反衬）

### 5.1 竞品能力矩阵（4 类 × 6 维表格）
- 分类：传统食谱 App(下厨房/豆果) / AI 食谱 / 营养健康管理 / 通用饮食助手。
- 6 维：个性化健康约束 / 食材识别闭环 / 结果可溯源 / 透明标注 / 长期记忆 / 膳食决策深度。
- 结论：**本项目在"个性化健康约束 + 可溯源 + 三层记忆"上差异化最明显**。

### 5.2 价值主张
- 健康中国 2030 / 慢病防控 / 控糖人群日常刚需（响应国家战略，契合 AC2 场景创新立意）。

---

## 六、应用前景与商业模式（AC2 偏技术，可精简）

### 6.1 应用场景
- C 端：控糖 / 慢病 / 健身 / 过敏人群做饭助手。
- B 端：社区健康 / 养老 / 企业食堂定制餐单（远期）。

### 6.2 商业模式（简述即可，AC2 不像 AC6 那样卡商业）
- C 端订阅（高级营养画像）/ B 端方案授权 / 与营养机构内容合作。
- 说明当前为"技术验证阶段"，商业模式非本赛重点。

---

## 七、演示脚本（3-5 分钟 Demo，可另附视频说明文字）

1. 开场：一句话定位 → 痛点场景（"我想吃个健康的一餐，但不知道该不该吃这个"）。
2. 零输入：上传一张冰箱照 → Agent 识别食材。
3. 健康护栏 A/B：同一问题"开护栏 vs 关护栏"，展示违规拦截 + 回灌。
4. 透明标注：展示 AI 图 / 来源链接 / 免责。
5. 记忆：第二轮对话 Agent 记得上轮画像（三层记忆）。

---

## 附件核对清单（提交前逐项打勾）

- [ ] 标题 ≤ 20 字，突出"Agent / 营养健康"
- [ ] 简介 ≤ 300 字（问题-方案-效果-应用）
- [ ] 技术方案 PDF ≤ 10M
- [ ] 演示视频 MP4，3-5 分钟，≤ 300MB
- [ ] 答辩 PPT（导出 PDF）
- [ ] 佐证材料合并 PDF（软著等）
- [ ] 代码包 → 百度网盘永久分享链接（命名：团队编号-赛题-作品名-材料名）
- [ ] **通读全文确认无校名 / Logo / 指导教师**

---

## 待你决策 / 待补项（先不填）

1. **最终锁定哪个子赛道**：本大纲按「AC2 AI+软件创新」或「AC4 AI+场景创新」写。二者材料几乎通用，仅叙事侧重不同（AC2 偏工程落地，AC4 偏场景方案）。请你定一个。
2. **项目是否已内置 RAG 检索**：是的话 2.6 单开一节挂数据；已弃用则标"见佐证"或删节。
3. **软件著作权**是否已在申请（佐证材料的关键）？要赶 10.15 省赛，越早申请越稳。
4. 验证数据（问卷/访谈/试用）进度到哪，是否已出可引用数字。
5. 桌面主文档 `私厨参赛.md`（plan 里提到的总文档）已不在桌面，是否要我重建一份整合版（本大纲的母稿）。
