# 修改执行日志

执行时间：2026-08-31 凌晨（无人值守自动任务）

| # | 状态 | 主要改动 | 验证 |
|---|---|---|---|
| 1 | 完成 | `.env` 增加 `HF_ENDPOINT`；新增 `scripts/download_bge.py`；`api/main_app.py` 预热失败日志明确为“知识库降级为仅文本检索” | 后端 import 冒烟通过 |
| 2 | 完成 | `sessions_store.py` 标题前缀清洗、首轮答案菜名写回、新增 `rename_session`；`session_route.py` 增加 PATCH；前端侧栏与聊天头支持行内改名 | 前端 build 通过 |
| 3 | 完成 | `feedback_store.py` 新增 `forget_dish`；`taste_store.py` 新增 `clear_signals_for`；`session_route.py` 增加忘记菜品 API；`FeedbackSummary.tsx` 提供“不再避开”按钮 | 前端 build 通过 |
| 4 | 完成 | `ChatArea.tsx` 增加本地 1 秒计时，不再直接展示后端 10 秒心跳 | 前端 build 通过 |
| 5 | 完成 | 后端补图线程新增 `image_failed` 事件与失败说明持久化；前端 `client.ts`/`App.tsx` 透传并把说明写入 `recipe.image_note`，`RecipeCard` 复用现有 `image-note` 块展示 | 前端 build 通过，后端 py_compile 通过 |
| 6 | 完成 | 反馈接口返回结构化 `down_items`；前端语义包装“已为你避开”，轮询 60s 改为 10s | 前端 build 通过 |
| 7 | 完成 | 用户消息不再渲染“你”标签，只保留“我”头像 | 前端 build 通过 |
| 8 | 完成 | `list_starred` 返回完整 `answer`；收藏页直接复用 `RecipeCard` 展示完整做法 | 前端 build 通过 |
| 9 | 不改 | 按文档决策保留统计聚合 | - |
| 10 | 完成 | 聊天区改为用户上翻不强制回底，新增右下角回底按钮与 CSS | 前端 build 通过 |
| 11 | 完成 | `App.tsx` 恢复历史 opening；`ChatArea.tsx` 增加“菜谱卡片 / 管家讲解”切换 | 前端 build 通过 |
| 12 | 完成 | `index.css` 补齐 `.family-editor select` 深色样式并允许滚动，避免底部裁切 | 前端 build 通过 |
| 13 | 完成 | 前端模式 4 改 3；删除采购清单、下周购物清单；定位改走 API 入参，不再混入聊天文本；出去吃面板按设计稿补齐预算/排序/数据来源/换一批；`nearby_route.py` 与 `agent_tools.py` 增加分页 | 前端 build 通过，后端 import 通过 |
| 14 | 完成 | `agent_graph.py` `verify_route` 增加追问闸，健康问答纯追问时路由 `plain -> END` | 后端 import 通过 |
| 补A | 完成 | `main.py` `build_human_message` 确定性注入冰箱库存 | 后端 import 通过 |
| 补B | 完成 | `/api/nearby` 支持 `page`；mock 按页轮转；前端“换一批”接线 | 前端 build 通过 |
| 补C | 完成 | `SessionSidebar.tsx` 增加定位权限块，可与聊天区通过 localStorage/事件同步 | 前端 build 通过 |
| 补D | 保留 | 工具栏定位指示保留 | - |

## 待用户决策

- 合盖动作设置项在系统电源方案中不存在，只关闭了睡眠/休眠；若硬件强制合盖睡眠，需要外接电源并保持系统不睡。
