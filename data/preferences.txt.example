# 用户长期偏好（公开模板，复制到 preferences.txt 后填写你自己真实的）
# 格式：每行一条有效偏好；以 # 开头的行为注释，会被自动忽略
# 修改后重启服务即可生效；文件缺失时程序不报错，只是没有偏好约束
#
# 复制命令（Linux/macOS）：
#   cp data/preferences.txt.example data/preferences.txt
# Windows PowerShell：
#   Copy-Item data/preferences.txt.example data/preferences.txt

# 忌口：不吃香菜、不吃动物内脏
# 辣度：中辣（推荐的菜需是重辣风格，步骤里说明放辣量）
# 减脂：否（为「是」时优先推荐低油低脂、高蛋白菜品，并标注减脂亮点）
# 糖尿病忌糖：否（为「是」时避免高糖食材与糖醋类做法，步骤中不额外加糖）
